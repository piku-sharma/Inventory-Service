<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
           http://www.liquibase.org/xml/ns/dbchangelog
           http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.23.xsd">

    <!-- ===================== -->
    <!-- 1. CREATE PRODUCT -->
    <!-- ===================== -->
    <changeSet id="001-create-product" author="sharma">
        <createTable tableName="product">
            <column name="product_id" type="BIGINT">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="product_name" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <!-- ===================== -->
    <!-- 2. CREATE BATCH -->
    <!-- ===================== -->
    <changeSet id="002-create-batch" author="sharma">
        <createTable tableName="batch">
            <column name="batch_id" type="BIGINT">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="product_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="quantity" type="INT">
                <constraints nullable="false"/>
            </column>

            <column name="expiry_date" type="DATE">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="batch"
                baseColumnNames="product_id"
                referencedTableName="product"
                referencedColumnNames="product_id"
                constraintName="fk_batch_product"/>
    </changeSet>

    <!-- ===================== -->
    <!-- 3. LOAD PRODUCT DATA -->
    <!-- ===================== -->
    <changeSet id="003-load-product-data" author="sharma">
        <loadData
                file="db/data/product.csv"
                tableName="product"
                separator=","
                encoding="UTF-8"/>
    </changeSet>

    <!-- ===================== -->
    <!-- 4. LOAD BATCH DATA -->
    <!-- ===================== -->
    <changeSet id="004-load-batch-data" author="sharma">
        <loadData
                file="db/data/batch.csv"
                tableName="batch"
                separator=","
                encoding="UTF-8"/>
    </changeSet>

</databaseChangeLog>
